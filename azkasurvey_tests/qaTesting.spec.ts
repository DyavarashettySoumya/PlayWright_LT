import {test} from "@playwright/test"

//--------------------------------------iframe----------------------------------
test("enter the details",async({page})=>{
    // //before automating an locator first identify the frame the create an variable for the frame.
    // //main frame(so create an variable for particular iframe)
    // await page.goto("http://localhost/QATesting/index.html");
    // let personalDetails=page.frameLocator("#personalFrame");
    // await personalDetails.locator("[name=`name`]").fill("playwright");
    
    // //another iframe
    // let professionalDetails=page.frameLocator("#professionalFrame");
    // await professionalDetails.locator("company").fill("LiveTech");

    // await personalDetails.locator("[name=`email`]").fill("playwright")
    // await page.waitForTimeout(3000);
    
})